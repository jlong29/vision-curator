# Edge to Curator Handoff

`thermal-data-engine` hands off Phase 2 packages. `vision-curator` treats those package roots as immutable inputs and records source paths plus provenance in its curator store.
